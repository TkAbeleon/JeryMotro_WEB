kind = "web"
previewPath = "/"
title = "JeryMotro Platform"
version = "1.0.0"
id = "artifacts/jerymotro"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18929

[services.development]
run = "pnpm --filter @workspace/jerymotro run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jerymotro", "run", "build" ]
publicDir = "artifacts/jerymotro/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18929"
BASE_PATH = "/"
